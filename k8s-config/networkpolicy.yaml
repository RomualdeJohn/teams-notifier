apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: sagt-teams-notification-networkpolicy
spec:
  podSelector:
    matchLabels:
      app: sagt-teams-notification
  policyTypes:
    - Egress
  egress:
    - ports:
        - protocol: TCP
          port: 443